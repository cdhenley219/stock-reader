import React from 'react';
//import { useSelector } from 'react-redux';
import './index.css';

const Stocks = () => {
   // const selectedStocks = useSelector(state => state.selectedDetailsList);

    return <div className="stocks">Stocks</div>
};

export default Stocks;